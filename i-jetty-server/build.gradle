
description = 'I-Jetty :: Jetty Adaptations for Android'

apply plugin: 'java'

repositories {
    mavenCentral()
    maven {
        url "http://files.couchbase.com/maven2/"
    }
    mavenLocal()
}

dependencies {
    compile group: 'org.eclipse.jetty', name: 'jetty-webapp', version:'7.6.0.RC4'
    compile group: 'org.eclipse.jetty', name: 'jetty-deploy', version:'7.6.0.RC4'
    compile(group: 'com.google.android', name: 'android', version:'1.6_r2') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
